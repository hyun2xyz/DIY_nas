{
  "name": "diy-nas",
  "version": "0.1.0",
  "description": "Sanitized DIY_nas pipeline template for Windows, Hyper-V, Ubuntu, ZFS, FileBrowser Quantum, and optional gateway integration.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "gateway": "node src/gateway-server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/server.mjs && node --check src/app.mjs && node --check src/gateway-server.mjs && node --check src/integration-gateway.mjs && node --check src/nextcloud-webdav.mjs",
    "audit:public": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/audit-public-release.ps1"
  },
  "engines": {
    "node": ">=20"
  }
}
